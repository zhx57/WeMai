# WeMai 项目依赖

# 微信自动化相关
uiautomation==0.4.0
Pillow==11.3.0
pywin32==311; sys_platform == "win32"
comtypes==1.4.11; sys_platform == "win32"
psutil==7.0.0
pyperclip==1.9.0

# WebSocket 支持（maim_message 依赖）
websockets==15.0.1

# MaiBot通信相关
# 新版麦麦(>=1.0)要求 maim_message>=0.6.2，旧版 0.3.x 用 socket.io 协议
# 会和新版麦麦的纯 WebSocket 服务端不兼容（连接 404）。
maim_message==0.7.5

# 工具
python-dotenv==1.1.1
