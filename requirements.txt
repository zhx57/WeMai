# WePush 项目依赖

# 微信自动化相关
uiautomation>=0.4.0
Pillow>=9.0.0
pywin32>=300
psutil>=5.9.0
pyperclip>=1.8.2

# 消息队列相关
redis>=4.5.0
aioredis>=2.0.0

# API服务相关
fastapi>=0.95.0
uvicorn>=0.22.0
requests>=2.28.0

# 异步支持
asyncio>=3.4.3
websockets>=11.0.0

# MaiBot通信相关
# 新版麦麦(>=1.0)要求 maim_message>=0.6.2，旧版 0.3.x 用 socket.io 协议
# 会和新版麦麦的纯 WebSocket 服务端不兼容（连接 404）。
maim_message>=0.6.2

# 日志和工具
datetime>=5.0
python-dotenv>=1.0.0