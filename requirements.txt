# WePush 项目依赖

# 微信自动化相关
uiautomation>=0.4.0
Pillow>=9.0.0
pywin32>=300
psutil>=5.9.0
pyperclip>=1.8.2

# 消息队列相关
# redis 5.x 默认会发 HELLO 命令做 RESP3 握手，老版本 Redis(<=5.x) 不支持
# 会报 "unknown command 'HELLO'"。锁定 <5.0 避免该问题。
# 代码用的是 from redis import asyncio as aioredis（redis 4.2+ 自带），
# 不再需要独立的 aioredis 包。
redis>=4.5.0,<5.0

# API服务相关
fastapi>=0.95.0
uvicorn>=0.22.0
requests>=2.28.0

# WebSocket 支持（maim_message 依赖）
websockets>=11.0.0

# MaiBot通信相关
# 新版麦麦(>=1.0)要求 maim_message>=0.6.2，旧版 0.3.x 用 socket.io 协议
# 会和新版麦麦的纯 WebSocket 服务端不兼容（连接 404）。
maim_message>=0.6.2

# 工具
python-dotenv>=1.0.0